#ifndef TYPES_H
#define TYPES_H

#include <list>
#include <vector>

class Triangle;
class Record;

typedef std::list<Triangle>::iterator tr_it;
typedef std::vector<Record*>::iterator rec_it;

#endif // TYPES_H
